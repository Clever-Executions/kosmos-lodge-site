[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PUBLIC_SANITY_PROJECT_ID = "j7q2a1is"
  PUBLIC_SANITY_DATASET    = "production"

# Catch-all 404 for static site
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
